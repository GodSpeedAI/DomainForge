name: Release NPM

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    name: Publish napi bindings to npm
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Decrypt secrets
        id: secrets
        uses: ./.github/actions/decrypt-secrets
        with:
          sops-age-key: ${{ secrets.SOPS_AGE_KEY }}

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build napi package
        run: npm run build

      - name: Patch generated TypeScript declarations
        run: python3 scripts/patch_napi_types.py

      - name: Smoke test npm pack
        run: |
          npm pack --dry-run 2>&1 | tee /tmp/npm-pack-output.txt
          grep -q "index.d.ts" /tmp/npm-pack-output.txt || { echo "ERROR: index.d.ts missing from pack"; exit 1; }
          grep -q "index.js" /tmp/npm-pack-output.txt || { echo "ERROR: index.js missing from pack"; exit 1; }
          grep -q "sea-core.*.node" /tmp/npm-pack-output.txt || { echo "ERROR: native .node binary missing from pack"; exit 1; }
          echo "npm pack smoke test passed"

      - name: Check if already published
        run: |
          if npm view "@domainforge/sea@$(node -p "require('./package.json').version")" version >/dev/null 2>&1; then
            echo "Already published, skipping"
            echo "SKIP_PUBLISH=1" >> $GITHUB_ENV
          fi

      - name: Publish to npm
        if: env.SKIP_PUBLISH != '1'
        run: npm publish --access public --ignore-scripts
        env:
          NODE_AUTH_TOKEN: ${{ steps.secrets.outputs.npm-token }}

  publish-wasm:
    name: Publish WASM package to npm
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Decrypt secrets
        id: secrets
        uses: ./.github/actions/decrypt-secrets
        with:
          sops-age-key: ${{ secrets.SOPS_AGE_KEY }}

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        run: cargo install wasm-pack --locked

      - name: Build WASM package
        run: |
          cd sea-core
          wasm-pack build --target web --release --scope domainforge -- --features wasm
          node - <<'NODE'
          const fs = require('fs');
          const pkgPath = 'pkg/package.json';
          const pkg = JSON.parse(fs.readFileSync(pkgPath, 'utf8'));
          pkg.name = '@domainforge/sea-wasm';
          pkg.files = ['sea_core.js', 'sea_core.d.ts', 'sea_core_bg.wasm', 'sea_core_bg.wasm.d.ts'];
          fs.writeFileSync(pkgPath, JSON.stringify(pkg, null, 2) + '\n');
          NODE

      - name: WASM smoke test
        run: |
          cd sea-core/pkg
          for f in sea_core.js sea_core.d.ts sea_core_bg.wasm sea_core_bg.wasm.d.ts package.json; do
            test -f "$f" || { echo "ERROR: missing $f in WASM pkg"; exit 1; }
          done
          node -e "const p=require('./package.json'); if (p.name !== '@domainforge/sea-wasm') throw new Error('Unexpected package name: ' + p.name); ['version','main','types'].forEach(k => { if(!p[k]) throw new Error('Missing field: ' + k) })"
          echo "WASM smoke test passed"

      - name: Check if WASM already published
        run: |
          cd sea-core/pkg
          if npm view "@domainforge/sea-wasm@$(node -p "require('./package.json').version")" version >/dev/null 2>&1; then
            echo "Already published, skipping"
            echo "SKIP_PUBLISH=1" >> $GITHUB_ENV
          fi

      - name: Publish WASM to npm
        if: env.SKIP_PUBLISH != '1'
        run: |
          cd sea-core/pkg
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ steps.secrets.outputs.npm-token }}
