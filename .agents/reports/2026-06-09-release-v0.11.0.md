# v0.11.0 Release Report

This report records evidence while preparing and publishing DomainForge v0.11.0.

## Initial Findings

- Branch: `dev`
- Upstream status: clean and even with `origin/dev`
- Previous release tag: `v0.10.0`
- Target release: `v0.11.0`
- GitHub CLI: authenticated as `SPRIME01`
- Remote `v0.11.0` tag/release: not found
- npm `@domainforge/sea@0.11.0`: not found
- npm `@domainforge/sea-wasm@0.11.0`: not found
- PyPI `sea-dsl`: latest observed version `0.10.0`
- Rust toolchain: available after `source ~/.cargo/env`

## Release Evidence

- Version bump:
  - Command: `source ~/.cargo/env; ./scripts/bump-version.sh 0.11.0 --no-commit`
  - Result: passed. Updated `sea-core/Cargo.toml`, `pyproject.toml`, and `package.json`.
  - Command: `source ~/.cargo/env; cargo update -p sea-core --precise 0.11.0`
  - Result: passed. Updated `Cargo.lock` from `sea-core v0.10.0` to `v0.11.0`.
- Release documentation:
  - Inputs: `git log v0.10.0..HEAD`, `git diff --stat v0.10.0..HEAD`, and changed-file inventory.
  - Result: updated `CHANGELOG.md` and `RELEASE_NOTES.md` with curated v0.11.0 notes.
- Validation:
  - Command: `source ~/.cargo/env; just all-tests`
  - Result: passed. Rust tests/doctests passed, Python tests passed, and TypeScript/Vitest passed with 13 files and 167 tests.
  - Command: `source ~/.cargo/env; PATH="$PWD/.venv/bin:$PATH"; ./scripts/build-release.sh`
  - Result: passed. Created local Linux CLI binary/archive, CPython 3.12 wheel, WASM archive, and `SHA256SUMS.txt`.
  - Command: `cd dist && sha256sum -c SHA256SUMS.txt && ./sea-0.11.0-linux-x86_64 --version`
  - Result: passed. All checksums OK; CLI printed `sea 0.11.0`.
  - Command: temporary venv install of `dist/sea_dsl-0.11.0-cp312-cp312-manylinux_2_34_x86_64.whl`
  - Result: passed. `import sea_dsl` reported version `0.11.0`.
  - Command: `tar tzf dist/sea-core-wasm-0.11.0.tar.gz`
  - Result: passed. Archive contains `README.md`, `package.json`, `sea_core.js`, `sea_core.d.ts`, `sea_core_bg.wasm`, and `sea_core_bg.wasm.d.ts`.
  - Command: `source ~/.cargo/env; PATH="$PWD/.venv/bin:$PATH"; npm run build`
  - Result: passed. napi release build completed and `scripts/patch_napi_types.py` reported no patches needed.
  - Command: manual equivalent of `just smoke-test-npm`
  - Result: passed. npm dry-run pack for `@domainforge/sea@0.11.0` included `index.js`, `index.d.ts`, `README.md`, `LICENSE`, and `sea-core.linux-x64-gnu.node`.
  - Command: `source ~/.cargo/env; cargo fmt --all --check`
  - Result: passed.
  - Command: `source ~/.cargo/env; cargo clippy -p sea-core --features cli -- -D warnings`
  - Result: passed.
  - Command: `python3 scripts/lint_release_workflows.py`
  - Result: passed.
  - Command: `source ~/.cargo/env; just ts-test`
  - Result: passed after generated `index.d.ts` update.

## Publish Outcome

- Release commits:
  - `d693508 chore: release v0.11.0`
  - `65bca82 ci: fix macOS release runners`
  - `a37c5ee ci: align release WASM size budget`
  - Follow-up workflow fixes on `dev`: `b7dad78 ci: fix release publish workflows`, `a4eea90 ci: harden PyPI cross-target publishing`, `b01e28e ci: publish intended WASM npm package`
  - Node/action maintenance and final tag-alignment fixes on `dev`: `a3e5b64 ci: update actions for Node 24 runners`, `773d557 ci: finish Node 24 action migration`, `ac4aa7f ci: avoid caching Rust build outputs`
- GitHub Release:
  - Tag: `v0.11.0`
  - Final tagged commit: `ac4aa7f064fd828f2efcae555fcec3ff6d06e90d`
  - URL: `https://github.com/GodSpeedAI/DomainForge/releases/tag/v0.11.0`
  - Release workflow runs: initial `27217939004`, retag refresh `27221146915`, and final `27223016012`; all completed successfully, with final assets built from `ac4aa7f064fd828f2efcae555fcec3ff6d06e90d`.
  - Assets verified with `gh release view v0.11.0 --json ...`: Linux, Windows, Intel macOS, Apple Silicon macOS, WASM archive, and four CPython 3.11 wheels.
- Crates.io:
  - Release workflow run: `27218456233`, result passed.
  - Verification command: `source ~/.cargo/env; cargo info sea-core@0.11.0`
  - Result: crates.io returned `sea-core` version `0.11.0`.
- PyPI:
  - Initial workflow-dispatch run `27218456178` failed on Windows checksum normalization and missing aarch64 Rust target.
  - Fixed by `b7dad78` and `a4eea90`, then reran workflow-dispatch run `27218923471` from `dev`.
  - Final result: passed for Linux x86_64, Linux aarch64, Windows x86_64, macOS Intel, and macOS Apple Silicon.
  - Verification command: `python3 -m pip index versions sea-dsl`
  - Result: PyPI returned latest `sea-dsl (0.11.0)`.
- npm:
  - Initial workflow-dispatch run `27218456182` failed because `bun` was missing in the native publish job.
  - Rerun `27218823080` reached npm publish but failed with registry E404; WASM package name was still generated as `@domainforge/sea-core`.
  - Fixed workflow setup and intended WASM package metadata in `b7dad78` and `b01e28e`.
  - Final rerun `27219127845` built and smoke-tested both tarballs, then failed at registry publish:
    - `@domainforge/sea@0.11.0`: `npm error 404 Not Found - PUT https://registry.npmjs.org/@domainforge%2fsea`
    - `@domainforge/sea-wasm@0.11.0`: `npm error 404 Not Found - PUT https://registry.npmjs.org/@domainforge%2fsea-wasm`
  - Verification command: `npm view @domainforge/sea@0.11.0 version; npm view @domainforge/sea-wasm@0.11.0 version`
  - Result: both returned E404.
- Post-fix CI:
  - Command: `gh run watch 27219116036 --exit-status`
  - Result: passed on `dev` commit `b01e28ea3adc6ff8d0cb51135ae05bc18929fb30` for lint/format, Rust on Ubuntu/Windows/macOS, Python 3.11/3.12, TypeScript, WASM, security audit, and minimal integration check.

## Residual Risks

- npm publication is not complete. The workflow-side package build, metadata patching, and smoke tests pass; the remaining failure is registry authorization or missing `@domainforge` npm scope/package setup.
- GitHub Actions emitted Node.js 20 action deprecation warnings and a `windows-latest` redirect notice. These did not block this release, but the workflows should be updated before GitHub's June and September 2026 runner changes.
  - Status update: follow-up commits `a3e5b64` and `773d557` updated first-party and release actions for Node 24 compatibility; final release run `27223016012` completed without the earlier Node 20 action warnings.

## Follow-up CI Maintenance

- Branch sync:
  - Command: `git status --short --branch && git rev-list --left-right --count origin/dev...dev`
  - Result: local `dev` is synchronized with `origin/dev` (`0 0`). The apparent mismatch was the release tag lagging behind later `dev` workflow fixes; `v0.11.0` now peels to `ac4aa7f064fd828f2efcae555fcec3ff6d06e90d`, matching `dev`.
- npm scope check:
  - Command: `npm org ls domainforge --json`
  - Result: `E404 Scope not found`, confirming npm publication still requires external npm organization/scope or token setup.
- Node.js 24 workflow updates:
  - Updated first-party actions to Node 24-compatible majors: `actions/checkout@v6`, `actions/setup-node@v6`, `actions/setup-python@v6`, `actions/cache@v5`, `actions/upload-artifact@v6`, and `actions/download-artifact@v7`.
  - Updated explicit Node toolchain jobs from Node 20/22 to Node 24.
  - Updated `extractions/setup-just` to `v4`.
  - Updated `softprops/action-gh-release` to `v3`.
  - Replaced `windows-latest` with `windows-2025-vs2026` to remove runner image redirect ambiguity.
  - Replaced `goto-bus-stop/setup-zig@v2` with direct Zig 0.11.0 download and SHA256 verification for PyPI ARM Linux publishing.
- Validation:
  - `python3 scripts/lint_release_workflows.py`: passed.
  - `npx --yes bun@latest install --frozen-lockfile`: passed.
  - `source ~/.cargo/env; npx --yes bun@latest run build`: passed.
  - `npx --yes bun@latest vitest run`: passed, 13 files and 167 tests.
  - `gh run watch 27222136576 --exit-status`: passed final CI on `dev` commit `ac4aa7f064fd828f2efcae555fcec3ff6d06e90d`.
  - `gh run watch 27223016012 --exit-status`: passed final Release workflow for tag `v0.11.0` at `ac4aa7f064fd828f2efcae555fcec3ff6d06e90d`.

## Final Verification

- Branch and tag:
  - `dev`, `origin/dev`, and `v0.11.0^{}` all resolve to `ac4aa7f064fd828f2efcae555fcec3ff6d06e90d`.
  - Remote tag object: `f89bd9ec8ee32bc4bfeaf6c3260678cd4dcd1247`; peeled commit: `ac4aa7f064fd828f2efcae555fcec3ff6d06e90d`.
- GitHub Release:
  - Final Release workflow run `27223016012` passed.
  - `gh release view v0.11.0 --json ...` reports nine assets: four CLI archives, one WASM archive, and four CPython 3.11 wheels.
  - Generated source archive check passed: `https://github.com/GodSpeedAI/DomainForge/archive/refs/tags/v0.11.0.tar.gz` contains a README matching local `dev` with SHA256 `6c5da561d3624189872b0fbde153ae6ea69a307a586c8181eff980d727a88d62`.
- Registries:
  - `source ~/.cargo/env; cargo info sea-core@0.11.0` from `/tmp` downloaded `sea-core v0.11.0` from crates.io.
  - `python3 -m pip index versions sea-dsl` reports `sea-dsl (0.11.0)`.
  - `npm view @domainforge/sea@0.11.0 version` and `npm view @domainforge/sea-wasm@0.11.0 version` still return E404 until npm scope/package access is fixed.
