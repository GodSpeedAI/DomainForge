{
  "description": "Entity versions + a breaking ConceptChange (v1->v2). Pins that the model parses and the ConceptChange/evolution metadata is captured in the canonical graph. Canonical validation enforcement is pinned by 13_evolution_enforcement.",
  "command": "parse",
  "input": "input.sea",
  "expected": "expected/graph.json"
}
